import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  icon: {
    fontSize: 25,
    backgroundColor: "#16222b",
  },
  tapBar: {
    backgroundColor: "#16222b",
  },
  totalCard: {
    position: "absolute",
    top: -5,
    right: -10,
  },
});
