import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  contaier: {
    padding: 20,
  },
  loading: {
    marginTop: 20,
  },
  noAddressText: {
    textAlign: "center",
    paddingTop: 20,
    fontSize: 28,
  },
});
